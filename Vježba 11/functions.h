#include <iostream>
#include <fstream>
#include <algorithm>
#include <vector>
#include <iterator>

using namespace std;

void read_file(vector<int>&);
bool check_greater(int);
void count_greater(vector<int>);
void min_max_el(vector<int>);
bool check_lower(int);
void delete_lower(vector<int>&);
void sort_descending(vector<int>&);
